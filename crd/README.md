See [../apis/README.md](../apis/README.md) for how to manage this directory.

TODO: `./checkpointconfiguration.yaml` should be generated from
`../api/types.go`, like resources are in k/k, but we haven't figured out how to
do that.
